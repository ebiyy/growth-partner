# 注意事項（最上部に配置）
caution:
  - トップレベルディレクトリ以外でのbun installは禁止
  - core変更時は必ず依存パッケージの検証が必要
  - ui変更時はweb/desktopの影響確認が必須
  - configの変更は全パッケージの再検証が必要
  - タスク完了時は必ずdocs配下の関連ドキュメントを更新
  - トークン使用量が80%を超えたら新しいセッションを検討

# トークン管理（即座に確認が必要な情報）
tokens:
  limit: 80k
  warning: 70k
  distribution:
    core: 25k
    api: 20k
    ui: 15k
    web: 15k
    desktop: 15k
    config: 10k

# パッケージ構成（基本構造の把握に必要）
packages:
  dependencies:
    core: []      # 独立
    api: [core]
    ui: []        # 独立
    web: [ui]
    desktop: [ui]
    config: []    # 独立

# 基本制約（違反するとビルドが失敗する制約）
constraints:
  package_manager: bun
  install:
    allowed: ["トップレベルディレクトリ"]
    prohibited: ["個別パッケージディレクトリ", "npm", "yarn", "pnpm"]
  
  priority:
    1: core
    2: api
    3: ui

# 自動読み込みファイル（セッション開始時に読み込み）
auto_load_files:
  # プロジェクト全体の構造とコンセプト
  - docs/index/README.md
  - docs/products/growth-partner-concept.md
  
  # パッケージとライブラリ情報
  - docs/packages/core.yml
  - docs/libraries/management.yml
  
  # プロセスと管理
  - docs/processes/error-handling.yml
  - docs/processes/session-management.yml
  - docs/tasks/README.md
  
  # AI特性とペルソナ定義
  - docs/persona/ME.yml
  - docs/persona/BEHAVIOR.yml
  - docs/persona/README.md

# 詳細情報の参照先（必要に応じて参照）
references:
  index: docs/index/          # ドキュメント構造の全体像
  processes: docs/processes/  # 開発プロセス
  packages: docs/packages/    # パッケージ詳細
  libraries: docs/libraries/  # ライブラリ情報
  products: docs/products/    # プロダクト情報
  tasks: docs/tasks/         # タスク管理
  persona: docs/persona/     # AI特性定義